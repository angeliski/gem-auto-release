RSpec.describe Gem::Auto::Release do
  it "has a version number" do
    expect(Gem::Auto::Release::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
